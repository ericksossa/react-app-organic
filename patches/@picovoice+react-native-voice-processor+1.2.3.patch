diff --git a/node_modules/@picovoice/react-native-voice-processor/react-native-voice-processor.podspec b/node_modules/@picovoice/react-native-voice-processor/react-native-voice-processor.podspec
index 9529917..795ae5a 100644
--- a/node_modules/@picovoice/react-native-voice-processor/react-native-voice-processor.podspec
+++ b/node_modules/@picovoice/react-native-voice-processor/react-native-voice-processor.podspec
@@ -28,7 +28,6 @@ Pod::Spec.new do |s|
         "CLANG_CXX_LANGUAGE_STANDARD" => "c++17"
     }
     s.dependency "React-Codegen"
-    s.dependency "RCT-Folly"
     s.dependency "RCTRequired"
     s.dependency "RCTTypeSafety"
     s.dependency "ReactCommon/turbomodule/core"
diff --git a/node_modules/@picovoice/react-native-voice-processor/ios/VoiceProcessor.swift b/node_modules/@picovoice/react-native-voice-processor/ios/VoiceProcessor.swift
--- a/node_modules/@picovoice/react-native-voice-processor/ios/VoiceProcessor.swift
+++ b/node_modules/@picovoice/react-native-voice-processor/ios/VoiceProcessor.swift
@@ -28,8 +28,10 @@ class PvVoiceProcessor: RCTEventEmitter {
     public override init() {
         super.init()
         voiceProcessor.addFrameListener(VoiceProcessorFrameListener({ frame in
+            // Normalize samples to Double to avoid mixed-type payloads in RN bridge events.
+            let normalizedFrame: [Double] = frame.map { Double(Int($0)) }
             DispatchQueue.main.async {
-                self.sendEvent(withName: self.frameEmitterKey, body: Array(frame))
+                self.sendEvent(withName: self.frameEmitterKey, body: normalizedFrame)
             }
         }))
 
@@ -59,6 +61,18 @@ class PvVoiceProcessor: RCTEventEmitter {
             frameLength: Int,
             sampleRate: Int,
             resolver resolve: RCTPromiseResolveBlock,
             rejecter reject: RCTPromiseRejectBlock) {
+        do {
+            let audioSession = AVAudioSession.sharedInstance()
+            try audioSession.setCategory(
+                    AVAudioSession.Category.playAndRecord,
+                    mode: AVAudioSession.Mode.measurement,
+                    options: [.allowBluetooth, .defaultToSpeaker])
+            try audioSession.setActive(true, options: .notifyOthersOnDeactivation)
+        } catch {
+            reject("PV_AUDIO_RECORDER_ERROR", "Unable to configure iOS audio session", error)
+            return
+        }
+
         do {
             try voiceProcessor.start(frameLength: UInt32(frameLength), sampleRate: UInt32(sampleRate))
         } catch {
